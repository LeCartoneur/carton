version: "3"
services:
  api:
    build: .
    image: api
    command: npm start
    restart: always
    env_file:
      - api.env
    ports:
      - 8000:8000
  mongo:
    image: mongo
    restart: always
    volumes:
      - db:/data/db

volumes:
  db:
